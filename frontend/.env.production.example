# Copy to Vercel → Project → Settings → Environment Variables
# Production + Preview + Development

VITE_API_BASE_URL=https://financeflow-api-gf0x.onrender.com

# Optional: agent-only web build (separate Vercel project)
# VITE_AGENT_APP=true
